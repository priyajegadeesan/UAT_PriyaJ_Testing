# Node Native Pipeline Test

This repository is used to validate:

- npm install installs devDependencies
- prepare lifecycle script executes
- postinstall lifecycle script executes
- Husky is disabled in CI using HUSKY=0
